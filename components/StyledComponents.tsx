import { styled } from '@mui/material'
import Typography from '@mui/material/Typography'

const BlendingTypography = styled(Typography)({
  mixBlendMode: 'difference'
})

export default BlendingTypography
